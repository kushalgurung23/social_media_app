class NewsNotice {
  final String senderId, senderName, senderImage, postedTime, noticeType;

  NewsNotice(
      {required this.senderId,
      required this.senderName,
      required this.senderImage,
      required this.postedTime,
      required this.noticeType});
}
